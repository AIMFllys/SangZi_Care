# ⚡ MCP 工具数量快速修复

> **问题：** 64 个工具 → 目标：50 个以下  
> **预计时间：** 5 分钟

---

## 🚀 快速修复（3 步）

### 步骤 1：检查用户配置

打开用户级别的 MCP 配置文件：

**Windows**:
```
C:\Users\[你的用户名]\.kiro\settings\mcp.json
```

**Mac/Linux**:
```
~/.kiro/settings/mcp.json
```

### 步骤 2：禁用所有服务器

如果文件存在，将所有服务器改为 `disabled: true`：

```json
{
  "mcpServers": {
    "任何服务器": {
      "disabled": true
    }
  }
}
```

或者直接删除/重命名该文件：
```bash
# Windows
ren "%USERPROFILE%\.kiro\settings\mcp.json" "mcp.json.backup"

# Mac/Linux
mv ~/.kiro/settings/mcp.json ~/.kiro/settings/mcp.json.backup
```

### 步骤 3：重新连接

在 Kiro 中：
1. 按 `Ctrl+Shift+P`
2. 输入 `MCP: Reconnect All Servers`
3. 等待连接完成

---

## ✅ 验证结果

查看 Kiro 状态栏，确认工具数量：
- ✅ 应该显示约 35 个工具
- ✅ 只有 3 个服务器连接（Supabase、Filesystem、Shadcn）

---

## 📊 预期效果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 工具数量 | 64 | ~35 |
| 启用的服务器 | 未知 | 3 |
| 性能 | ❌ 差 | ✅ 优 |
| Token 消耗 | ❌ 高 | ✅ 低 |

---

## 🔄 如果问题仍然存在

### 方案 A：使用优化配置

复制优化后的配置：
```bash
cp .kiro/settings/mcp-optimized.json .kiro/settings/mcp.json
```

### 方案 B：手动检查

1. 打开 Kiro 侧边栏
2. 找到 "MCP Servers" 面板
3. 手动禁用不需要的服务器

### 方案 C：重置配置

备份并重置：
```bash
# 备份
cp .kiro/settings/mcp.json .kiro/settings/mcp.json.backup

# 只保留核心配置
```

然后手动编辑 `.kiro/settings/mcp.json`，只保留：
- Supabase
- Filesystem
- Shadcn

---

## 📚 详细文档

需要更多信息？查看：
- [MCP 工具优化指南](MCP_OPTIMIZATION_GUIDE.md)
- [MCP 完整配置教程](MCP_COMPLETE_SETUP_GUIDE.md)

---

*快速修复指南 · 桑梓智护项目*
