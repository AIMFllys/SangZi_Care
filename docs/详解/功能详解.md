# 桑梓智护 — 功能详解与现状

> 最后更新: 2026-07-13
> 产品名: 智护银龄 · 包名 `sangzi-smart-care`  
> 架构方向: EdgeOne 全栈 Next.js · Android 在线壳 · 业务 API 在 `app/api`（历史 `backend/` 已删除）  
> 配套: [项目结构详解](./项目结构详解.md) · [目标架构](../designs/target-architecture.md) · [tech-debt](../issues/tech-debt.md)

本文按功能域说明「做什么 / 谁用 / 代码落点 / 成熟度 / 已知缺口」。成熟度标签统一为：

| 标签 | 含义 |
|------|------|
| **已实现** | 主路径可端到端走通（UI → Store/Hook → API → 持久化） |
| **部分实现** | 核心能力有，但入口、播放、权限或原生桥接不完整 |
| **基础设施未挂载** | 库/Hook/Store 已写好，应用壳或页面未调用 |
| **占位** | 仅有 UI 跳转、Mock 或后端占位返回，产品闭环未完成 |

---

## 一、总览矩阵

| 功能域 | 成熟度 | Elder | Family | 关键落点 |
|--------|--------|-------|--------|----------|
| 认证与角色 | 已实现 | ✓ | ✓ | `app/login` · `app/onboarding` · `hooks/useAuth` |
| 双端首页 | 已实现 | 语音优先 | 看板优先 | `ElderHomeView` · `FamilyHomeView` |
| 健康记录 | 已实现 | ✓ | ✓（同页） | `app/health` · `healthStore` |
| 健康录入 | 已实现 | 主入口 | 可直达 URL | `app/health/input` · `useVoiceRecognition` |
| 用药管家 | 已实现 | Tab 中心 | 同页 | `app/medicine` · `medicineStore` |
| 用药计划表单 | 基础设施未挂载 | — | 组件就绪 | `components/medicine/PlanForm`（无路由） |
| 捂话消息 | 已实现 | ✓ | ✓ | `app/messages` · `messageStore` |
| 家庭绑定 | 已实现 | 生成绑码 | 输入绑码 | `app/settings/bind` · `familyStore` |
| 设置 / 资料 | 部分实现 | ✓ | ✓ | `app/settings/*` |
| 无障碍 | 已实现 | ✓ | 可访问 | `app/settings/accessibility` |
| AI 语音助手 | 部分实现 | 首页入口 | Tab 中心 | `app/voice` · `useAIChat` |
| 健康广播 | 部分实现 | 无 Tab | 无 Tab | `app/radio` · `radioStore` |
| 紧急呼叫 | 占位（前端）/ API 就绪 | SOS 跳设置 | — | `app/api/v1/emergency` |
| 离线同步 | 基础设施未挂载 | — | — | `lib/offlineSync.ts` |
| Supabase Realtime | 基础设施未挂载 | — | — | `hooks/useRealtimeSync` |
| Android 壳 | 部分实现 | ✓ | ✓ | `android/` · `lib/jsbridge.ts` |

**明确不在当前产品范围**（旧 PRODUCT_SPEC 有、代码无）：点餐、语音唤醒词「小护」、手机号短信登录。

---

## 二、认证与角色

**成熟度: 已实现**

### 用户故事
新用户用邮箱完成验证码登录；首次进入选择 Elder / Family 角色；之后按角色切换主题与 TabBar。

### 流程

```
访问受保护路由
  → useAuth → userStore.initialize()
  → 无 token → /login
  → 有 token、无 role → /onboarding
  → 正常 → 目标页
```

登录页：数学图形验证码 → 发邮箱 OTP → 校验 → 写入 `token` / `refresh_token` → 跳转。

### 代码落点

| 层 | 路径 |
|----|------|
| UI | `app/login/page.tsx` · `app/onboarding/page.tsx` |
| Hook | `hooks/useAuth.ts` |
| Store | `stores/userStore.ts` |
| API | `GET /api/v1/auth/captcha` · `POST /api/v1/auth/send-code` · `POST /api/v1/auth/verify` · `POST /api/v1/auth/refresh` |
| 用户 | `GET/PATCH /api/v1/users/me` · `PATCH /api/v1/users/me/role` |
| 表 | `oc_users` |

### 已知缺口
- OTP / 验证码存进程内存，重启丢失，不适合多实例
- `test_auth.py` / `test-api.http` 仍有旧「手机号」痕迹，与现网不一致

---

## 三、双端首页

**成熟度: 已实现**

### Elder（`components/home/ElderHomeView.tsx`）
- 问候、时间卡、天气文案（当前为硬编码占位）
- 大语音球 → `/voice`
- SOS 按钮 → `/settings`（紧急闭环未接）

### Family（`components/home/FamilyHomeView.tsx`）
- 绑定老人列表切换
- 用药进度摘要、心率趋势、血压告警卡
- 位置/状态文案仍有硬编码（如「在家休息」）

### 分流
`app/page.tsx`：`user.role === 'family'` → `FamilyHomeView`，否则 → `ElderHomeView`。

主题由 `ClientShell` 根据 `localStorage.user_role` 设置 `data-theme`。

---

## 四、健康记录

**成熟度: 已实现**（跨用户权限门控偏弱）

### 看板 `/health`
展示心率、血压、体温等最新值；双端共用同一页面，默认查**当前登录用户**数据。

### 录入 `/health/input`
- 手动录入多种体征
- 语音录入走 `useVoiceRecognition`（Web Speech → Native Bridge → 豆包转写三级降级）
- 中文数字解析写入记录

### 代码落点

| 层 | 路径 |
|----|------|
| UI | `app/health/page.tsx` · `app/health/input/page.tsx` |
| 组件 | `HealthCard`（有测试，页面未引用） |
| Store | `stores/healthStore.ts` · `types/health.ts` |
| API | `POST/GET /api/v1/health/records` · `/latest` · `/trend` |
| 表 | `health_records` |

### 已知缺口
- 后端支持 `?user_id=`，但缺少「是否已绑定且 can_view_health」的强制校验
- 家属首页趋势与 `/health` 页数据源需注意一致性

---

## 五、用药管家

**成熟度: 已实现**（计划 CRUD UI 未挂路由）

### 今日提醒 `/medicine`
- 今日时间线、确认服药
- Elder Tab 中心入口；Family 可同页访问

### 历史 `/medicine/history`
- 活跃 / 历史计划分组
- 无 Tab 直达，需从页面内或 URL 进入

### 未挂载组件
| 组件 | 状态 |
|------|------|
| `PlanForm` | 表单完整 + 有测试，**无页面路由** |
| `ReminderModal` | 有实现，页面用内联提醒 UI |
| `MedicineTimeline` | 有实现，页面未引用 |

### API / 表
- `GET/POST/PATCH /api/v1/medicine/plans`
- `GET /api/v1/medicine/today`
- `POST /api/v1/medicine/records`
- `POST /api/v1/medicine/notify-family`（通知为 stub，无真实推送）
- 表：`medication_plans` · `medication_records`

---

## 六、捂话（家庭消息）

**成熟度: 已实现**

### 能力
- `/messages`：联系人列表（来自家庭绑定 + 最近消息）
- `/messages/[id]`：文字发送；语音发送保留 16kHz 单声道 WAV、PCM 推导的真实时长与转写
- 语音文件由服务端校验为规范 RIFF/WAVE PCM16LE 后写入私有 Supabase Storage，消息表只保存稳定对象路径
- 会话响应只暴露同源鉴权播放地址；播放路由校验 sender/receiver 后生成 10 分钟签名 URL
- 录音、转写和上传阶段均可取消；客户端下载真实音频后播放，切换/结束/卸载会中止请求并清理 Blob URL
- 未读计数、已读标记

### 代码落点
- UI：`app/messages/*` · `MessageList` · `VoiceRecorder` · `ChatBubble`
- Store：`messageStore` · `lib/messageUtils.tsx`
- API：`/api/v1/messages/*`（含 multipart `send-voice`）· `/api/v1/voice/audio`
- 表：`oc_elder_care_messages`；Storage bucket：`SUPABASE_VOICE_BUCKET`（private）

### 运行约束
- Supabase 控制台必须预建 private bucket 并在本地/EdgeOne 配置 `SUPABASE_VOICE_BUCKET`；上传和签名前都会确认 bucket 存在且 `public === false`
- 上传上限 5 MiB、PCM 录音上限 60 秒，客户端时长声明与 PCM 允许 1 秒缓冲偏差；Storage 或入库失败不会追加消息，入库失败会补偿删除对象

---

## 七、家庭绑定

**成熟度: 已实现**（权限字段存在不一致风险）

### 能力
- Elder：生成 6 位绑码
- Family：输入绑码 + 关系，建立 `active` 绑定
- 权限开关（查看健康 / 紧急通知等）、解绑

### 代码落点
- UI：`app/settings/bind/page.tsx` · `app/family/[id]/`
- Hook：`useFamilyBinds`
- Store：`familyStore`
- API：`/api/v1/family/generate-code` · `/bind` · `/binds` · `PATCH/DELETE binds/{id}`
- 表：`elder_family_binds`

### 已知缺口
- 绑定 API 写布尔列（如 `can_receive_emergency`），紧急/用药通知部分逻辑读 JSONB `permissions.*`，可能导致通知目标为空
- `familyStore` 展示名有时用 `relation` 占位，非对方真实姓名

---

## 八、设置、资料与无障碍

**成熟度: 核心语音闭环已实现**（意图动作分发仍未挂载）

| 页面 | 状态 |
|------|------|
| `/settings` | 用户卡、分组入口、退出登录；「消息通知 / 关于我们」为即将推出 |
| `/settings/profile` | 姓名、生日、性别、慢性病 — 已实现 |
| `/settings/bind` | 见上节 — 已实现 |
| `/settings/accessibility` | 字号预览 + 语速，经 API 持久化 — 已实现 |

无障碍页文档常标「Elder」，代码无角色守卫，Family 也可打开。

---

## 九、AI 语音助手

**成熟度: 部分实现**

### 已通
- `/voice` 文本对话走 `useAIChat` → `POST /api/v1/ai/chat`（豆包；未配置 Key 时服务端有占位回复）
- 意图识别 API：`POST /api/v1/ai/intent`
- 会话摘要 API：`GET /api/v1/ai/summary/{user_id}`（`summaryStore` 存在但 UI 未用）
- `/voice` 已接真实 16kHz 单声道 PCM WAV 录音、MiMo ASR 和 MiMo MP3 TTS；录音、转写、播放均可取消并隔离迟到结果
- 服务端实现位于 `lib/server/mimo.ts` 与 `app/api/v1/voice/*`；密钥只读 `MIMO_API_KEY`，所有重试共享 45 秒总截止时间
- 用药提醒、健康语音录入和家庭消息均复用同一套真实语音能力；聊天语音原文件另存私有 Storage

### 未通 / 弱
| 项 | 现状 |
|----|------|
| `intentHandlers.ts` | 可把意图落到健康/用药/消息/紧急，**仅测试引用，页面未接** |
| `VoiceBall` / `VoicePanel` | 组件存在；Elder 首页内联语音球，未复用组件 |

### 入口差异
- Elder：首页语音球 → `/voice`（TabBar 在 `/voice` 隐藏）
- Family：Tab 中心为 `/voice`

---

## 十、健康广播

**成熟度: 已实现**（内容生成入口当前无 Tab）

- 页面：`app/radio/page.tsx` — 推荐列表、分类网格、真实音频播放与无音频禁用态
- Store：`radioStore` 用 `Audio` 控制播放、暂停、跳转、结束记录和卸载清理，并隔离旧媒体迟到事件
- API：已迁入 `app/api/v1/radio/`（`GET /recommend` · `GET /categories` · `POST /play-record` · `POST /generate`）
- `POST /generate` 使用 MiMo 生成真实 MP3，先上传 private `SUPABASE_VOICE_BUCKET`，再把稳定对象路径写入 `oc_health_broadcasts`；入库失败补偿删除对象
- `GET /recommend` 鉴权后把稳定路径换成 10 分钟签名 URL，响应始终 `private, no-store`，不会把内部路径发给浏览器
- **无 Tab 入口**；主要靠常量/直达 URL

---

## 十一、紧急呼叫

**成熟度: 前端占位 · API 就绪**

### API（已迁入 Next，CRUD 形态）
- 已迁入 `app/api/v1/emergency/`
- `POST /api/v1/emergency/trigger` · `/cancel` · `/notify`
- `GET /api/v1/emergency/history`
- 表：`emergency_calls`
- 无真实电话/推送通道，以写库 + 解析通知对象为主

### 前端
- **无** `EmergencyFAB`、**无** `useEmergency`、**无** `/emergency` 路由
- Elder 首页 SOS → `/settings`，带 TODO 性质跳转

---

## 十二、离线同步与 Realtime

**成熟度: 基础设施未挂载**

| 模块 | 文件 | 说明 |
|------|------|------|
| 离线队列 | `lib/offlineSync.ts` | 有实现与单测；`ClientShell` 未 `startOfflineSync` |
| Realtime | `lib/realtimeSubscriptions.ts` · `hooks/useRealtimeSync.ts` | 可订 `health_records` 等表；应用从未挂载 Hook |
| 汇总 | `stores/summaryStore.ts` | AI 摘要；Family 首页未用 |

挂载时订阅表名须为 `oc_*`（与 [`lib/realtimeSubscriptions.ts`](../../lib/realtimeSubscriptions.ts) / [ops/oc-table-prefix.md](../ops/oc-table-prefix.md) 一致）。

---

## 十三、Android WebView 壳

**成熟度: 部分实现（已定为模式 B）**

- WebView / 浏览器打开 **线上 https**（EdgeOne 或调试 URL），**不再**内嵌静态 `out/`
- 前端 `lib/jsbridge.ts` 期望 `AndroidBridge`；原生为 `SangZiBridge` → 契约不一致（TD-20）
- Native TTS/ASR 在桥对齐前不可用；Web Speech / 服务端占位可降级

详见 [android/README.md](../../android/README.md)。

---

## 十四、端到端能力矩阵（Elder vs Family）

| 能力 | Elder | Family |
|------|-------|--------|
| 登录 / 选角色 | ✓ | ✓ |
| 首页 | 语音球 + SOS 占位 | 多老人看板 |
| Tab：消息 | 「亲属」 | 「消息」 |
| Tab：中心 | 用药 `/medicine` | 语音 `/voice` |
| 健康看板 | ✓ | ✓（当前用户数据） |
| 健康录入 | 主路径 | URL 可达 |
| 用药今日/确认 | ✓ | ✓ |
| 用药计划表单 | 无入口 | 组件未挂 |
| 捂话 | ✓ | ✓ |
| 生成绑码 | ✓ | — |
| 输入绑码 | — | ✓ |
| 家人详情 | ✓ | ✓（健康摘要更丰富） |
| 广播 | 无 Tab | 无 Tab |
| 紧急呼叫产品闭环 | ✗ | ✗ |
| 无障碍设置 | ✓ | 可访问 |

---

## 十五、已知缺口优先级（文档记录，本次不改代码）

1. **紧急呼叫前端闭环** — FAB / 状态机 / 接 `trigger` API  
2. **JSBridge 契约对齐** — `AndroidBridge` ↔ `SangZiBridge`  
3. **`/voice` 真实 ASR + intentHandlers 挂载**  
4. **`useRealtimeSync` / `offlineSync` 挂入 ClientShell**  
5. **用药 `PlanForm` 路由化**（家属代管计划）  
6. **家庭权限字段统一**（布尔列 vs JSONB）+ 健康/用药 `user_id` 鉴权  
7. **广播播放接线**与音频存储  
8. ~~**静态导出动态路由**深链策略~~ — 已自然消解（全栈 SSR/CSR，不再静态导出）  
9. ~~**Schema / `oc_` 前缀**与运行时代码对齐~~ — 已完成（2026-07，`types/supabase.ts` + `app/api` + Realtime 表名）

---

## 十六、与结构 / 架构文档的关系

- 「终局怎么走」→ [目标架构](../designs/target-architecture.md)  
- 「在哪个目录、怎么分层」→ [项目结构详解](./项目结构详解.md)  
- 「能做什么、做到哪」→ 本文  
- 「欠什么」→ [tech-debt](../issues/tech-debt.md)  

标签与路径应保持一致；架构或功能变更时同步更新日期。
